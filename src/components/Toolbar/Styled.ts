import styled from 'styled-components';

export const StyledToolbar = styled.div`
  display: flex;
  height: 60px;
  background: var(--tertiary);
  box-shadow: 0px 0px 36px #00000066;
`;

export const TitleToolbar = styled.h2`
  color: var(--light);
  padding-left: 65px;
  padding-top: 15px;
  width: 50%;
`;

export const User = styled.h2`
  color: var(--light);
  padding-right: 20px;
  padding-top: 20px;
  width: 50%;
  text-align: right;
  font-size: 1.2rem;
`;
